import React from 'react';

import { HConfirmationDialog, HConfirmationDialogProps } from '@hybrain/mui.ui.dialogs.h-confirmation-dialog';

const ConfirmationDialog: React.FC<HConfirmationDialogProps> = (props) => {
	return <HConfirmationDialog {...props} />;
};

export default ConfirmationDialog;
