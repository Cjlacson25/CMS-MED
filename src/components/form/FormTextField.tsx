import React from 'react';
import { HFormTextField, HFormTextFieldProps } from '@hybrain/mui.ui.formik.h-form-text-field';

const FormTextField: React.FC<HFormTextFieldProps> = (props) => {
	return <HFormTextField {...props} />;
};

export default FormTextField;
